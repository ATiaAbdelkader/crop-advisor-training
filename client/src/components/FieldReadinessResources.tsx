import { fieldReadinessResources, localIntelligenceSteps } from "@shared/fieldReadiness";
import { BadgeDollarSign, MapPinned, MessageCircleMore, ScanLine, ShieldCheck } from "lucide-react";

const icons = [BadgeDollarSign, MessageCircleMore, ScanLine];

export default function FieldReadinessResources() {
  return <><section className="mt-7 rounded-[24px] border border-[#d9e6d5] bg-[#f1f7ee] p-6 sm:p-7"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-[#658164]">Algeria-relevant local intelligence</p><h2 className="mt-2 font-serif text-2xl font-semibold text-[#2d4834]">Build a current local advisory picture before you advise.</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-[#58705a]">Use locality-specific evidence rather than a fixed national calendar, assumed price, or generic legal statement. Record the source, its date, uncertainty, and the condition that requires an update in your practicum or capstone response.</p><div className="mt-5 grid gap-3 md:grid-cols-5">{localIntelligenceSteps.map((step, index) => <div key={step.title} className="rounded-xl border border-[#dbe7d7] bg-white p-4"><span className="font-serif text-xl font-semibold text-[#3e7248]">{index + 1}</span><p className="mt-2 text-sm font-bold text-[#36533b]">{step.title}</p><p className="mt-2 text-xs leading-5 text-[#657a67]">{step.prompt}</p></div>)}</div><p className="mt-5 rounded-xl bg-white/80 p-4 text-xs leading-5 text-[#547056]"><ShieldCheck className="mr-2 inline h-4 w-4" />For regulatory, product, seed, food-safety, or label questions, verify the current official or label source before action. Course content is not legal approval.</p></section><section className="mt-7 grid gap-4 md:grid-cols-3">{fieldReadinessResources.map((resource, index) => { const Icon = icons[index] ?? MapPinned; return <article key={resource.id} className="rounded-[22px] border border-[#dfe7dc] bg-[#fcfcf8] p-6"><Icon className="h-5 w-5 text-[#4d7e56]" /><p className="mt-5 text-[10px] font-bold uppercase tracking-[0.16em] text-[#698068]">Field method resource</p><h2 className="mt-2 font-serif text-xl font-semibold text-[#2e4934]">{resource.title}</h2><p className="mt-3 text-sm leading-6 text-[#566c58]">{resource.standard}</p><div className="mt-5 rounded-xl bg-[#f3f8f0] p-4"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-[#698068]">Try it in your next visit</p><p className="mt-2 text-sm leading-6 text-[#466447]">{resource.practice}</p></div></article>; })}</section></>;
}
