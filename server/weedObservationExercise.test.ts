import{expect,it}from"vitest";import{weedObservationComplete,weedObservationExerciseRequirements}from"../shared/weedObservationExercise";it("keeps weed observation non-prescriptive",()=>{expect(weedObservationExerciseRequirements.safetyBoundary).toContain("does not identify a weed");expect(weedObservationComplete({pattern:"a",crop:"a",prevention:"a",identity:"a",next:"a"})).toBe(true)})
