import{expect,it}from"vitest";import{fieldExerciseCatalog,parseExerciseProgress}from"../shared/exerciseProgress";it("lists voluntary exercises and safely reads browser completion data",()=>{expect(fieldExerciseCatalog).toHaveLength(14);expect(parseExerciseProgress("invalid")).toEqual({});expect(parseExerciseProgress('{"/crop-walk-lab":{"completed":2,"total":3,"updatedAt":1}}')["/crop-walk-lab"]?.completed).toBe(2)})
