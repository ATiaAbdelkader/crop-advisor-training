import{expect,it}from"vitest";import{ipmDecisionComplete,ipmDecisionExerciseRequirements}from"../shared/ipmDecisionExercise";it("keeps IPM exercise non-prescriptive",()=>{expect(ipmDecisionExerciseRequirements.safetyBoundary).toContain("does not establish action thresholds");expect(ipmDecisionComplete({prevention:"a",monitoring:"a",identification:"a",options:"a",escalation:"a"})).toBe(true)})
